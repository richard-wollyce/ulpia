# Installed and built, both derived. ADR-0003's rule applied to the frontend:
# the source is tracked, everything reproducible from it is not.
node_modules/
dist/

# Generated from content/posts by tools/build-posts.mjs. Derived and disposable:
# deleting it costs a rebuild, never a post.
blog/
public/blog/
